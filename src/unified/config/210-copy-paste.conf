# Use M-c and M-v for copy/paste
bindsym Alt+c $exec_env $i3scripts/send-copy.sh
bindsym Alt+v $exec_env $i3scripts/send-paste.sh

# alt copy and paste keys if one hand is on the mouse
bindsym Alt+j $exec_env $i3scripts/send-copy.sh
bindsym Alt+k $exec_env $i3scripts/send-paste.sh
